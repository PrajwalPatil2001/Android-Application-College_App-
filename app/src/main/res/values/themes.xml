<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.College_app" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/headcolor</item>
        <item name="colorPrimaryVariant">@color/headcolor</item>
        <item name="android:statusBarColor">@color/headcolor</item>
    </style>

    <style name="Theme.College_app" parent="Base.Theme.College_app" />
</resources>